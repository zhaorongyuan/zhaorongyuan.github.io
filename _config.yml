# Hexo 配置文件
## 文档: https://hexo.io/docs/configuration.html
## 源码: https://github.com/hexojs/hexo/

# 网站信息 (Site)
title: 1s2s3         # 网站标题
subtitle: ''         # 网站副标题
description: ''      # 网站描述（用于SEO）
keywords:            # 关键词
author: OGN          # 作者名字
language: zh-CN      # 语言（建议修改为 zh-CN）
timezone: ''         # 时区（例如：Asia/Shanghai）

# 网址设置 (URL)
## 在这里设置你的网站网址。
## 例如，如果你使用 GitHub Page，将 url 设置为 'https://username.github.io/project'
## 如果绑定了域名，就填你的域名
url: http://1s2s3.com  # 🚨 记得改成你的真实域名或 GitHub Pages 地址
permalink: :year/:month/:day/:title/
permalink_defaults:
pretty_urls:
  trailing_index: true # 设置为 false 以从永久链接中移除 trailing 'index.html'
  trailing_html: true  # 设置为 false 以从永久链接中移除 trailing '.html'

# 目录设置 (Directory)
source_dir: source       # 资源文件夹（文章、图片等）
public_dir: public       # 生成的静态网页文件夹
tag_dir: tags            # 标签文件夹
archive_dir: archives    # 归档文件夹
category_dir: categories # 分类文件夹
code_dir: downloads/code # 代码文件夹
i18n_dir: :lang          # 国际化文件夹
skip_render:             # 跳过渲染的文件

# 文章写作设置 (Writing)
new_post_name: :title.md # 新文章的文件名格式
default_layout: post     # 默认布局
titlecase: false         # 是否将标题转换为标题大写
external_link:
  enable: true           # 在新标签页打开外部链接
  field: site            # 应用于整个网站
  exclude: ''            # 排除的链接
filename_case: 0         # 文件名大小写转换 (0: 不转换, 1: 小写, 2: 大写)
render_drafts: false     # 是否渲染草稿
post_asset_folder: false # 是否启动资源文件夹（每篇文章一个同名文件夹放图片）
relative_link: true     # 是否使用相对链接
future: true             # 是否显示未来的文章
syntax_highlighter: highlight.js # 代码高亮插件
highlight:
  line_number: true      # 显示行号
  auto_detect: false     # 自动检测语言
  tab_replace: ''        # Tab 替换
  wrap: true             # 代码换行
  hljs: false            # 这里的 false 是指不给 css 类名加 hljs 前缀
prismjs:                 # 另一种代码高亮方案
  preprocess: true
  line_number: true
  tab_replace: ''

# 首页设置 (Home page setting)
# path: 博客索引页的根路径 (默认为 '')
# per_page: 每页显示的文章数 (0 = 关闭分页)
# order_by: 文章排序 (默认按日期降序)
index_generator:
  path: ''
  per_page: 10
  order_by: -date

# 分类 & 标签 (Category & Tag)
default_category: uncategorized # 默认分类
category_map:                   # 分类映射
tag_map:                        # 标签映射

# 元数据元素 (Metadata elements)
## https://developer.mozilla.org/en-US/docs/Web/HTML/Element/meta
meta_generator: true

# 日期 / 时间格式 (Date / Time format)
## Hexo 使用 Moment.js 来解析和显示日期
## 你可以根据 http://momentjs.com/docs/#/displaying/format/ 自定义日期格式
date_format: YYYY-MM-DD
time_format: HH:mm:ss
## updated_option 支持 'mtime' (修改时间), 'date' (创建时间), 'empty' (空)
updated_option: 'mtime'

# 分页设置 (Pagination)
## 将 per_page 设置为 0 以禁用分页
per_page: 10
pagination_dir: page

# 包含 / 排除文件 (Include / Exclude file(s))
## include:/exclude: 选项仅适用于 'source/' 文件夹
include:
exclude:
ignore:

# 扩展 (Extensions)
## 插件: https://hexo.io/plugins/
## 主题: https://hexo.io/themes/
# theme: landscape
theme: redefine

# 部署设置 (Deployment)
## 文档: https://hexo.io/docs/one-command-deployment
deploy:
  # 方案一：部署到阿里云服务器 (使用 SSH)
  - type: git
    # 你的阿里云服务器配置
    repo: ssh://root@60.205.167.213/var/repo/hexo_blog.git
    branch: master
  
  # 方案二：部署到 GitHub Pages
  - type: git
    # 使用 HTTPS 链接 (如果你网络好)
    repo: https://github.com/zhaorongyuan/zhaorongyuan.github.io.git
    # 💡 提示：如果总是连接超时，可以把上面这行改成 SSH 格式：
    # repo: git@github.com:zhaorongyuan/zhaorongyuan.github.io.git
    branch: main